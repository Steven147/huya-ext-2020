const projectDir = '{{ BUILDER_NEORN_ENV_projectDir }}'
const baseConfigAbs = '{{ BUILDER_NEORN_ENV_baseConfigAbs }}'

const resolvePaths = [process.cwd(), projectDir]

const { mergeConfig } = require(require.resolve('metro-config', { paths: resolvePaths }))

module.exports = mergeConfig(
  require(baseConfigAbs),
  {},
  // require('@hyext/css2js-metro-plugin/css2js.config.js'),
)
