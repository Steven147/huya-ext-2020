const path = require('path')

module.exports = {
  SDKVersion: '1.9.0',
  staticServer: {
    port: 18080,
  },
  metroServer: {
    port: 8081,
  },
  builderTempDir: path.resolve(__dirname, '.builder-h5game-temp'),
}
